import { defineConfig } from "vitepress";

export default defineConfig({
  title: "VitePress Versions - V2",
  description: "V2 of the showcase of VitePress Versions with Git tags.",
  themeConfig: {
    socialLinks: [
      { icon: "github", link: "https://github.com/its-miroma/vpv-tag" },
    ],
  },
});
