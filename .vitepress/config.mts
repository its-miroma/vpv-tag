import { defineConfig } from "vitepress";

export default defineConfig({
  title: "VitePress Versions - V4",
  description: "V4 of the showcase of VitePress Versions with Git tags.",
  themeConfig: {
    socialLinks: [
      { icon: "github", link: "https://github.com/its-miroma/vpv-tag" },
    ],
  },
});
